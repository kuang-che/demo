# Flask Server Configuration
FLASK_ENV=development
FLASK_APP=backend/server.py
PORT=5000

# SQLite Database Location
# Relative paths are resolved starting from the backend/ directory.
DB_PATH=../database/tasks.db
